const canvas=document.querySelector("#vertex-canvas"),ctx=canvas.getContext("2d");
let w,h,points=[],mouse={x:innerWidth*.72,y:innerHeight*.45};
function size(){w=canvas.width=innerWidth*devicePixelRatio;h=canvas.height=innerHeight*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);points=Array.from({length:innerWidth<700?25:52},(_,i)=>({x:innerWidth*(.46+Math.random()*.57),y:80+Math.random()*(innerHeight-160),r:Math.random()*1.4+.3,vx:(Math.random()-.5)*.16,vy:(Math.random()-.5)*.16,a:Math.random()*.6+.15}))}
function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);points.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<innerWidth*.42||p.x>innerWidth+20)p.vx*=-1;if(p.y<40||p.y>innerHeight-40)p.vy*=-1;let dx=p.x-mouse.x,dy=p.y-mouse.y,d=Math.hypot(dx,dy);if(d<180){p.x+=dx/d*.18;p.y+=dy/d*.18}ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(255,105,55,${p.a})`;ctx.fill();points.forEach(q=>{let dist=Math.hypot(p.x-q.x,p.y-q.y);if(dist<130){ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(q.x,q.y);ctx.strokeStyle=`rgba(210,215,208,${(1-dist/130)*.1})`;ctx.lineWidth=.5;ctx.stroke()}})});requestAnimationFrame(draw)}
addEventListener("resize",size);addEventListener("pointermove",e=>{mouse.x=e.clientX;mouse.y=e.clientY;document.querySelector(".cursor-glow").style.transform=`translate(${e.clientX-210}px,${e.clientY-210}px)`});size();draw();
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("in");observer.unobserve(e.target)}}),{threshold:.16});document.querySelectorAll(".reveal").forEach(el=>observer.observe(el));
const countObserver=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){const n=+e.target.dataset.count;let v=0;const t=setInterval(()=>{e.target.textContent=++v;if(v>=n)clearInterval(t)},140);countObserver.unobserve(e.target)}}));document.querySelectorAll("[data-count]").forEach(el=>countObserver.observe(el));
document.querySelectorAll(".cap-item").forEach(btn=>btn.addEventListener("click",()=>{document.querySelectorAll(".cap-item").forEach(b=>b.classList.remove("active"));btn.classList.add("active")}));
document.querySelector(".menu-toggle").addEventListener("click",e=>{const open=e.currentTarget.getAttribute("aria-expanded")==="true";e.currentTarget.setAttribute("aria-expanded",!open);document.body.classList.toggle("menu-open",!open)});
document.querySelectorAll(".desktop-nav a").forEach(a=>a.addEventListener("click",()=>{document.body.classList.remove("menu-open");document.querySelector(".menu-toggle").setAttribute("aria-expanded","false")}));
document.querySelectorAll(".magnetic").forEach(el=>{el.addEventListener("pointermove",e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.06}px,${(e.clientY-r.top-r.height/2)*.1}px)`});el.addEventListener("pointerleave",()=>el.style.transform="")});
